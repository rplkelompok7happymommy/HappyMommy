<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.happymommy.happymommy">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Login"
            android:label="HAPPY MOMMY"/>
        <activity
            android:name=".Registrasi"
            android:label="Registrasi" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".Home"
            android:label="@string/title_activity_home"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Akun"
            android:label="@string/title_activity_akun"
            android:parentActivityName=".Home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".LupaPassword"
            android:parentActivityName=".Login"/>
        <activity
            android:name=".ChangePassword"
            android:parentActivityName=".Home" />
        <activity
            android:name=".OpsiKesehatan"
            android:parentActivityName=".Home" />
        <activity android:name=".JenisKesehatan" />
        <activity android:name=".Fragment.TipsActivity" />
        <activity android:name=".Fragment.MakananActivity" />
        <activity android:name=".Fragment.KondisiActivity" />
        <activity android:name=".Fragment.GejalaActivity" />
        <activity
            android:name=".Senam"
            android:screenOrientation="landscape"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".EditProfile"
            android:parentActivityName=".Akun"/>
        <activity
            android:name=".InfoRumahSakit"
            android:parentActivityName=".Home" />
        <activity android:name=".DetailRumahSakit" />
        <activity android:name=".CommentActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />

        <uses-library android:name="com.google.android.maps" />

        <activity
            android:name=".MenuKontraksi"
            android:parentActivityName=".Home" />
        <activity
            android:name=".AboutUs"
            android:parentActivityName=".Home" />

    </application>

</manifest>